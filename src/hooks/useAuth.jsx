// import { useSelector } from 'react-redux'

// const useAuth = () => {
//   const { email, id, phoneNumber } = useSelector((state) => state.user)

//   return {
//     isAuth: Boolean(email),
//     email,
//     id,
//     phoneNumber,
//   }
// }

// export default useAuth
